<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.grouptwo.ustcdiary">

    <application
        android:name=".topicActivity.contact.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".topicActivity.contact.FindActivity"></activity>
        <activity android:name=".topicActivity.memoryActivity" />
        <activity android:name=".topicActivity.diaryActivity" />
        <activity android:name=".topicActivity.contact.contactActivity" />
        <activity android:name=".topicActivity.contact.AddData" />
        <activity
            android:name=".main.mainActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".init.initActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>